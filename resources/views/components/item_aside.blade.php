<li class="pl-4 mt-6 py-2">
    <a href="{{ route($enlace) }}" class="flex items-center gap-3 cursor-pointer hover:mix-blend-soft-light hover:scale-105">
        <img src="{{ asset($icono) }}">
        <span class="hidden lg:block text-white">{{$test}}</span>
    </a>
</li>
